# Environment

> 本文档回答：这个项目通常在哪运行？运行前需要满足什么环境前提？

## 执行环境摘要（待填写）

- 默认运行环境：`local / slurm / docker / k8s / cloud VM / 其他`
- 包管理方式：`conda / venv / poetry / uv / 其他`
- 主要执行入口：`python / bash / make / sbatch / srun / 其他`
- 典型运行位置：`本地终端 / 登录节点 / 计算节点 / 容器内`

## 资源与系统约束（待填写）

- 操作系统 / 发行版：
- Python / 运行时版本：
- GPU / CUDA 要求：
- 存储约束（共享盘 / 本地盘 / 临时目录）：
- 网络约束（可外网 / 仅内网 / 无网）：

## 必要前置步骤（待填写）

1. 激活环境：例如 `conda activate agent`
2. 加载模块或依赖：例如 `module load cuda`
3. 切换运行位置：例如“训练必须在 Slurm 计算节点执行”
4. 其他前置：环境变量、凭据、挂载目录

## 调度与作业约定（待填写）

- 是否必须通过调度器运行：
- 调度器类型：`slurm / k8s / 其他 / 当前未配置`
- 提交命令：
- 交互命令：
- 日志查看方式：

## 最小验证命令（待填写）

```bash
# 例：确认当前环境可运行最小检查
<command>
```

## 已知限制（待填写）

- 例：登录节点禁止直接跑长任务
- 例：仅特定队列可申请 GPU
- 例：必须先激活 conda 环境，否则依赖缺失

## 维护规则

1. 任何会影响执行方式的环境事实都应写在这里，而不是只留在聊天上下文中。
2. 若后续 run 发现环境与本文档不一致，必须立即更新本文档，并在必要时补写 `docs/decisions.md`。
3. 若某项环境事实尚未确认，明确写“待确认”或“当前未配置”，禁止隐式假设。
